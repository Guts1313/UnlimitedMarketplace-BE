build_application:
  image: docker:19.03.12
  variables:
    JAVA_HOME: "/usr/lib/jvm/java-11-openjdk-amd64" # Example path, adjust as needed
  script:
    - echo $JAVA_HOME # This is just to demonstrate that JAVA_HOME is set
    - ./gradlew build # Example build command
